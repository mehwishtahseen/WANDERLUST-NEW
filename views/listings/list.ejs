<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>List</title>
  </head>
  <body>
    <h2><%= list.title %></h2>
    <img style="width: 200px" src="<%= list.image.url %>" alt="" />
    <p><%= list.description %></p>
    <h4>Price : &#8377; <%= list.price.toLocaleString("en-IN") %></h4>
    <h5>Location : <%= list.location %></h5>
    <h5>Country : <%= list.country %></h5>
    <a href="/listings/<%= list._id %>/edit">Update</a>
    <form action="/listings/<%= list._id %>?_method=DELETE" method="post">
      <button>Delete</button>
    </form>
    <hr />
  </body>
</html>
